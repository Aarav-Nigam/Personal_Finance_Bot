[project]
name = "personal-finance-bot"
version = "0.1.0"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.ruff.lint.per-file-ignores]
"ui/glossary.py" = ["E501"]

[tool.ruff.format]
quote-style = "double"
